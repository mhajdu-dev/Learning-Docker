# Global options block at the very top
{
    log {
        level DEBUG
    }
}

# Site block
http://auth.localhost:8008 {

     handle / {
         # REMOVED: file_server off
         reverse_proxy ui:3000 {
             header_up Host {http.reverse_proxy.upstream.hostport}
         }
     }

     # Keep other handlers commented out
     # handle /sockjs-node/* { ... }
     # handle /api/* { ... }
}